name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 # সংস্করণ v2 থেকে v4 এ পরিবর্তন করা হয়েছে
      - uses: actions/setup-java@v4 # সংস্করণ v2 থেকে v4 এ পরিবর্তন করা হয়েছে
        with:
          java-version: '11'
          distribution: 'adopt'
      - run: npm install -g cordova
      - run: |
          cordova create build_dir com.writer.sheshjosna "Shesh Josna"
          cp -r * build_dir/www/ || true
          cp config.xml build_dir/config.xml
          cd build_dir
          cordova platform add android
          cordova build android --debug
      - uses: actions/upload-artifact@v4 # সংস্করণ v2 থেকে v4 এ পরিবর্তন করা হয়েছে
        with:
          name: my-app
          path: build_dir/platforms/android/app/build/outputs/apk/debug/app-debug.apk
